{
  "name": "mantle-wrapped-backend",
  "version": "1.0.0",
  "description": "Backend for Mantle Wrapped — AI wallet analysis + IPFS upload",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.24.0",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
